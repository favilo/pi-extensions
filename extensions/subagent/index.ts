import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function subagentExtension(_pi: ExtensionAPI): void {
  // Registration is introduced by the first production tracer slice.
}
